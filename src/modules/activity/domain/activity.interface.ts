export interface activityI {
  activity: string;
  client: string;
  initDate: string;
  finalDate: string;
  initTime: string;
  finalTime: string;
  user: string;
  randomid: string;
}

export interface stateI {
  state: string;
}
